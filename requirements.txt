# Runtime dependencies. Requires Python 3.10+ and a system Chrome/Chromium
# (rendering is done with Chrome directly).
# Install with: pip install -r requirements.txt
numpy>=2.0
pillow>=10.0
fastapi>=0.116,<1.0
httpx>=0.28,<1.0
uvicorn[standard]>=0.35,<1.0
